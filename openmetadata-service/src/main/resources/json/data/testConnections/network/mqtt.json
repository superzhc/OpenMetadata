{
  "name": "Mqtt",
  "displayName": "MQTT Test Connection",
  "description": "This Test Connection validates the access against the server.",
  "steps": [
    {
      "name": "CheckAccess",
      "description": "Validate that we can connect mqtt and authenticate with the given credentials.",
      "errorMessage": "Failed to connect to mqtt, please validate the credentials",
      "shortCircuit": true,
      "mandatory": true
    }
  ]
}